This file is used to test if docs are updated based on merged pull request.

Previous tests are failing because of a files.map function not found error let's see if they work now

okay signed up to ngrok now i have consistent url

forgot to restart node server after fixing bug

I moved the server.js to the correct directory so it's methods could access the .env file

correct the data received name from files to data

testing again after adding logging to my docgeneration file

testing overview Generation again after pulling new changes from main

test if architecture mermaid file gets generated with no errors.

